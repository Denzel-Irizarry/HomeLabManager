{
  "Api": {
    // When running from Visual Studio use the API HTTPS endpoint the API launchSettings binds to
    "BaseUrl": "https://localhost:7053"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
